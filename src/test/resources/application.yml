spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: test

novari:
  kafka:
    topic:
      org-id: test-org-id
      domain-context: test-domain-context
    default-replicas: 1
    application-id: test