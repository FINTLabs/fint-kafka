spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: test
    bootstrap-servers: ${spring.embedded.kafka.brokers}

fint:
  kafka:
    default-replicas: 1
    application-id: test