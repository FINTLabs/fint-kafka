spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: test

fint:
  kafka:
    enable-ssl: false

novari:
  kafka:
    topic:
      org-id: test-org-id
      domain-context: test-domain-context
    default-replicas: 1
    application-id: test