spring:
  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      properties:
        spring:
          json:
            trusted:
              packages: "*"
fint:
  Kafka:
    resourceRefreshCron: 0 0 0 */6 * *
    resourceRefreshDuration: 6d